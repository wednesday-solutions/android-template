rootProject.name = "Android Template"
include ':app',
        ':service',
        ':service-impl',
        ':service-di',
        ':service-entity'

rootProject.children.each {
    it.buildFileName = it.name + ".gradle"
}

